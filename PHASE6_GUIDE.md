# راهنمای فاز 6 - بهینه‌سازی و مستندات نهایی

## خلاصه

فاز 6 بهینه‌سازی‌ها و مستندات نهایی را برای آماده‌سازی بازی جهت استفاده در کلاس درس اضافه می‌کند.

---

## ویژگی‌های پیاده‌سازی شده

### 1. سیستم جلوگیری از اسپم (Anti-spam)

**مشکل:** کاربران می‌توانند در حین انیمیشن‌ها کلیک‌های متعدد انجام دهند و باگ ایجاد کنند.

**راه‌حل:** مکانیزم قفل دقیق با `is_processing`

#### پیاده‌سازی:

**قبل از فاز 6:**
```python
def input(self, key):
    if self.hovered and key == 'left mouse down':
        if not self.is_flipped:
            if game_manager and not game_manager.is_processing:
                self.flip()
                game_manager.on_card_flipped(self)
```

**بعد از فاز 6:**
```python
def input(self, key):
    if self.hovered and key == 'left mouse down':
        if not self.is_flipped and not self.is_matched:
            if game_manager and not game_manager.is_processing:
                # قفل کردن بازی در حین انیمیشن
                game_manager.is_processing = True
                
                # پخش صدا و چرخش
                if game_manager.audio_manager:
                    game_manager.audio_manager.play('click')
                
                self.flip()
                
                # باز کردن قفل بعد از 0.3 ثانیه
                invoke(lambda: self._unlock_after_flip(), delay=0.3)
                
                game_manager.on_card_flipped(self)

def _unlock_after_flip(self):
    """باز کردن قفل فقط اگر دو کارت باز نشده"""
    if game_manager:
        if len(game_manager.flipped_cards) < 2:
            game_manager.is_processing = False
```

#### زمان‌بندی قفل:

```
کلیک کارت 1:
├─ قفل (0.0s)
├─ انیمیشن چرخش (0.3s)
└─ باز کردن قفل (اگر < 2 کارت)

کلیک کارت 2:
├─ قفل (0.0s)
├─ انیمیشن چرخش (0.3s)
├─ بررسی تطبیق (0.5s)
└─ موفقیت/شکست:
    ├─ موفق: باز کردن فوری
    └─ ناموفق: تأخیر 1.5s سپس باز کردن
```

**مجموع زمان قفل:**
- کارت اول: 0.3 ثانیه
- کارت دوم: 0.3 + 0.5 = 0.8 ثانیه
- در صورت اشتباه: +1.5 ثانیه = 2.3 ثانیه

---

### 2. راهنمای داخل بازی (In-game Help)

**هدف:** کمک به دانش‌آموزان برای یادگیری قوانین بازی

#### کلاس HelpWindow:

```python
class HelpWindow(Entity):
    def __init__(self):
        super().__init__()
        self.ui_elements = []
        self.create_help()
    
    def create_help(self):
        # عنوان
        title = Text(text='❓ راهنمای بازی', ...)
        
        # قوانین (15 خط)
        rules_lines = [
            '📌 هدف بازی:',
            'پیدا کردن جفت کارت‌های یکسان',
            '',
            '🎮 نحوه بازی:',
            '۱. روی یک کارت کلیک کن',
            '۲. روی کارت دیگری کلیک کن',
            '۳. اگر یکسان باشند، می‌مانند',
            '۴. اگر متفاوت باشند، پنهان می‌شوند',
            '',
            '⭐ نکات مهم:',
            '• سعی کن جای اعداد را یادت بماند',
            '• با هر جفت درست، امتیاز می‌گیری',
            '• در بازی چند نفره، نوبت‌ها عوض می‌شود',
            '',
            '🎯 بازنده نداریم! همه یاد می‌گیرند! 🎯'
        ]
        
        # ایجاد Text برای هر خط با رنگ و اندازه مناسب
        for line in rules_lines:
            # تشخیص نوع خط و تنظیم استایل
            ...
        
        # دکمه بستن
        close_btn = Button(text='✓ فهمیدم!', ...)
```

#### محتوای راهنما:

**بخش 1: هدف بازی**
- توضیح ساده: "پیدا کردن جفت کارت‌های یکسان"

**بخش 2: نحوه بازی**
- 4 مرحله ساده
- شماره‌گذاری با اعداد فارسی
- توضیح واضح هر مرحله

**بخش 3: نکات مهم**
- نکات یادگیری
- سیستم امتیازدهی
- نوبت‌ها در چند نفره

**بخش 4: پیام مثبت**
- "بازنده نداریم!"
- تشویق یادگیری

#### ویژگی‌های طراحی:

```python
# رنگ‌ها بر اساس نوع خط
if line.startswith('📌') or line.startswith('🎮') or line.startswith('⭐'):
    scale = 1.8
    text_color = color.yellow  # عناوین
elif line.startswith('🎯'):
    scale = 1.6
    text_color = color.green   # پیام پایانی
else:
    scale = 1.3
    text_color = color.white   # متن عادی
```

---

### 3. مستندات دوزبانه (Bilingual Documentation)

**هدف:** دسترسی برای کاربران فارسی‌زبان و انگلیسی‌زبان

#### ساختار README جدید:

```markdown
# بازی حافظه کودکان | Kids Memory Game

## 🎮 درباره بازی | About the Game
### فارسی
...
### English
...

## 📥 نصب و راه‌اندازی | Installation
### فارسی
...
### English
...

## 🎯 راهنمای بازی | Game Guide
### فارسی
...
### English
...

## 🔧 سیستم مورد نیاز | System Requirements
...

## 🐛 رفع مشکلات | Troubleshooting
### فارسی
...
### English
...

## 🎓 استفاده در کلاس | Classroom Use
### فارسی
...
### English
...
```

#### مشخصات:

- **تعداد خطوط:** 574 (بود: 250)
- **زبان‌ها:** فارسی و انگلیسی
- **بخش‌های اصلی:** 15
- **کد بلاک‌ها:** 20+
- **نمادهای Emoji:** 50+

#### بخش‌های کلیدی:

**1. نصب (Installation)**
```bash
# فارسی
1. نصب Python
2. دانلود بازی
3. نصب وابستگی‌ها
4. اجرای بازی

# English
1. Install Python
2. Download the game
3. Install dependencies
4. Run the game
```

**2. راهنمای بازی (Game Guide)**
- چگونه بازی کنیم؟
- قوانین بازی
- تنظیمات
- راهنما
- پایان بازی
- نکات برای معلمان

**3. رفع مشکلات (Troubleshooting)**
- بازی اجرا نمی‌شود
- گوینده کار نمی‌کند
- تصاویر نمایش داده نمی‌شوند
- خطای Module not found

---

### 4. بهینه‌سازی حافظه (Memory Optimization)

**مشکل:** ذرات confetti و entities کارت‌ها در حافظه باقی می‌مانند

#### پیاده‌سازی:

**قبل از فاز 6:**
```python
def return_to_menu(self):
    for card in self.cards:
        destroy(card)
    
    for ui_text in self.ui_texts:
        destroy(ui_text)
    
    destroy(self.turn_text)
    destroy(self.level_text)
    ...
```

**بعد از فاز 6:**
```python
def return_to_menu(self):
    # 1. پاکسازی ذرات confetti
    for entity in scene.entities[:]:  # کپی لیست
        if isinstance(entity, ConfettiParticle):
            destroy(entity)
    
    # 2. پاکسازی کارت‌ها و فرزندانشان
    for card in self.cards:
        # حذف تصاویر و متن‌های فرزند
        if hasattr(card, 'number_image') and card.number_image:
            destroy(card.number_image)
        if hasattr(card, 'number_text') and card.number_text:
            destroy(card.number_text)
        if hasattr(card, 'back_text') and card.back_text:
            destroy(card.back_text)
        destroy(card)
    
    # 3. پاکسازی UI elements
    for ui_text in self.ui_texts:
        destroy(ui_text)
    
    destroy(self.turn_text)
    destroy(self.level_text)
    destroy(self.feedback_text)
    destroy(self.scoreboard_bg)
    destroy(self.scoreboard_title)
    
    if hasattr(self, 'back_btn'):
        destroy(self.back_btn)
    
    # 4. پاکسازی لیست‌ها
    self.cards.clear()
    self.ui_texts.clear()
    self.flipped_cards.clear()
    
    # 5. بازگشت به منو
    game_manager = None
    if game_menu:
        game_menu.show()
```

#### نکات مهم:

**1. کپی لیست در حلقه:**
```python
# اشتباه: تغییر لیست در حین حلقه
for entity in scene.entities:
    if isinstance(entity, ConfettiParticle):
        destroy(entity)  # خطا!

# درست: استفاده از کپی
for entity in scene.entities[:]:
    if isinstance(entity, ConfettiParticle):
        destroy(entity)  # OK
```

**2. بررسی وجود قبل از destroy:**
```python
if hasattr(card, 'number_image') and card.number_image:
    destroy(card.number_image)
```

**3. پاکسازی لیست‌ها:**
```python
self.cards.clear()      # نه self.cards = []
self.ui_texts.clear()   # پاکسازی مرجع‌ها
```

#### مزایا:

- ✅ جلوگیری از نشت حافظه
- ✅ پاکسازی کامل entities
- ✅ حذف مرجع‌های اضافی
- ✅ عملکرد بهتر در سیستم‌های ضعیف‌تر

---

### 5. تست نهایی JSON (Final JSON Test)

**هدف:** اطمینان از عملکرد صحیح ذخیره‌سازی

#### فایل test_phase6.py:

```python
def test_json_loading():
    """تست بارگذاری فایل JSON تاریخچه"""
    
    # تست 1: ایجاد فایل
    test_data = {
        'games': [{
            'date': '2026-02-01 06:00:00',
            'winner': 'بازیکن 1',
            'game_time': 45.67,
            'num_players': 2,
            'scores': [5, 3]
        }]
    }
    with open('game_results.json', 'w') as f:
        json.dump(test_data, f)
    
    # تست 2: بارگذاری فایل
    with open('game_results.json', 'r') as f:
        loaded_data = json.load(f)
    
    # تست 3: بررسی ساختار
    assert 'games' in loaded_data
    assert isinstance(loaded_data['games'], list)
    
    # تست 4: بررسی نوع داده‌ها
    game = loaded_data['games'][0]
    assert isinstance(game['date'], str)
    assert isinstance(game['winner'], str)
    assert isinstance(game['game_time'], (int, float))
    assert isinstance(game['num_players'], int)
    assert isinstance(game['scores'], list)
    
    # تست 5: افزودن بازی جدید
    new_game = {...}
    loaded_data['games'].append(new_game)
    with open('game_results.json', 'w') as f:
        json.dump(loaded_data, f)
    
    # تست 6: مدیریت خطا
    # ایجاد فایل نامعتبر و بررسی JSONDecodeError
```

#### تست‌های انجام شده:

1. ✅ **ایجاد فایل JSON**
   - بررسی امکان نوشتن
   - تنظیم encoding=utf-8
   - ensure_ascii=False برای فارسی

2. ✅ **بارگذاری فایل JSON**
   - خواندن فایل
   - parse کردن JSON
   - بررسی موفقیت

3. ✅ **بررسی ساختار داده‌ها**
   - کلید 'games' موجود است
   - مقدار یک لیست است
   - فیلدهای مورد نیاز موجودند

4. ✅ **بررسی نوع داده‌ها**
   - date: string
   - winner: string
   - game_time: number
   - num_players: integer
   - scores: list

5. ✅ **افزودن بازی جدید**
   - append به لیست
   - نوشتن مجدد فایل
   - بارگذاری و تأیید

6. ✅ **مدیریت خطا**
   - شناسایی JSON نامعتبر
   - JSONDecodeError
   - عدم crash برنامه

#### نتایج تست:

```bash
$ python test_phase6.py

======================================================================
         تست فاز 6 - بهینه‌سازی و مستندات
======================================================================

تست 1: بررسی وجود و ساختار فایل JSON...
  ✓ ایجاد فایل JSON موفق
  ✓ بارگذاری فایل JSON موفق
  ✓ ساختار داده‌ها صحیح است
  ✓ نوع داده‌ها صحیح است
  ✓ افزودن بازی جدید موفق

تست 2: مدیریت خطا...
  ✓ خطای JSON به درستی شناسایی شد

======================================================================
خلاصه نتایج تست
======================================================================

✓ تمام تست‌های JSON با موفقیت انجام شد!

======================================================================
         تست ویژگی‌های کد فاز 6
======================================================================

  ✓ پنجره راهنما
  ✓ نمایش راهنما
  ✓ دکمه راهنما
  ✓ باز کردن قفل بعد از چرخش
  ✓ قفل در input
  ✓ پاکسازی ذرات
  ✓ پاکسازی لیست‌ها
  ✓ محتوای راهنما

✓ تمام ویژگی‌های فاز 6 پیاده‌سازی شده است!

======================================================================
                    نتیجه نهایی
======================================================================
  JSON Loading: ✓ موفق
  Code Features: ✓ موفق

🎉 تمام تست‌های فاز 6 با موفقیت انجام شد! 🎉
```

---

## جمع‌بندی فاز 6

### آمار نهایی:

**کد:**
- متدهای تغییر یافته: 5
- متدهای جدید: 2
- کلاس جدید: 1 (HelpWindow)
- خطوط اضافه شده: ~200

**مستندات:**
- README جدید: 574 خط (بود: 250)
- زبان‌ها: 2 (فارسی، انگلیسی)
- بخش‌ها: 15 اصلی
- کد بلاک‌ها: 20+

**تست:**
- فایل تست: test_phase6.py (8KB)
- تست‌های JSON: 6
- بررسی‌های کد: 8
- نرخ موفقیت: 100%

### ویژگی‌های کلیدی:

✅ **سیستم ضد اسپم**
- قفل دقیق در حین انیمیشن‌ها
- مکانیزم باز کردن کنترل شده
- جلوگیری از کلیک‌های اضافی

✅ **راهنمای داخلی**
- قوانین ساده برای کودکان
- استایل جذاب با رنگ و نماد
- دکمه بستن راحت

✅ **مستندات دوزبانه**
- پوشش کامل فارسی/انگلیسی
- دستورالعمل‌های نصب
- راهنمای بازی
- رفع مشکلات
- راهنمای کلاس درس

✅ **بهینه‌سازی حافظه**
- پاکسازی کامل entities
- حذف ذرات
- پاکسازی لیست‌ها
- جلوگیری از نشت حافظه

✅ **تست نهایی**
- تست‌های جامع JSON
- بررسی ساختار داده
- مدیریت خطا
- تضمین کیفیت

---

## استفاده در کلاس درس

### برای معلمان:

**1. قبل از بازی:**
- راهنما را به دانش‌آموزان نشان دهید
- تنظیمات را مناسب سطح کلاس تنظیم کنید
- گوینده را برای دانش‌آموزان ضعیف‌تر فعال کنید

**2. در حین بازی:**
- دانش‌آموزان می‌توانند هر زمان از راهنما استفاده کنند
- سیستم ضد اسپم از باگ‌ها جلوگیری می‌کند
- حافظه بهینه برای سیستم‌های ضعیف

**3. بعد از بازی:**
- نتایج در game_results.json ذخیره می‌شود
- می‌توانید پیشرفت را پیگیری کنید
- آمارهای مفید برای ارزیابی

### برای دانش‌آموزان:

- دکمه راهنما همیشه در دسترس است
- قوانین به زبان ساده نوشته شده
- پیام‌های مثبت و تشویق‌کننده
- بدون نیاز به کمک بزرگسال‌ها

---

## نتیجه‌گیری

فاز 6 بازی را برای استفاده واقعی در کلاس درس آماده کرده است:

- ✅ بدون باگ با سیستم ضد اسپم
- ✅ راهنمای واضح برای کودکان
- ✅ مستندات کامل برای معلمان
- ✅ عملکرد بهینه برای سیستم‌های مختلف
- ✅ تست شده و تأیید شده

**بازی آماده است! 🎓🎮✅**
